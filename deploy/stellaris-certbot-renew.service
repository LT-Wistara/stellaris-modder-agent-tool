[Unit]
Description=Renew Stellaris MCP public IP certificate
After=network-online.target nginx.service

[Service]
Type=oneshot
ExecStart=/opt/certbot-venv/bin/certbot renew --quiet --deploy-hook "/usr/bin/systemctl reload nginx"
